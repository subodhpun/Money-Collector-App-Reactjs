import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gurudev.sahakari',
  appName: 'gurudev-sahakari-app',
  webDir: 'build'
};

export default config;
